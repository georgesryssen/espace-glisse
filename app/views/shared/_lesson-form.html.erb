<%= simple_form_for lesson do |f| %>
  <%= f.input :date, html5: true, label: "Date et heure" %>
  <%= f.input :duration, label: "Durée du cours" %>
  <%= f.input :category, label: "Sport", as: :grouped_select, collection: Category::CATEGORY.map { |sport| [sport[:field], sport[:sports].sort]}, group_method: :last %>
  <%= f.association :spot, label: "Spot" %>
  <%= f.input :school, as: :hidden, input_html: { value: current_user.school }%>
  <div class="text-center">
    <%= f.submit "Ajouter ce cours", class: 'mt-3 btn btn-flat' %>
  </div>
<% end %>
